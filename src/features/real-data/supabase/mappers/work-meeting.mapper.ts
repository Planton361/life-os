import type { WorkMeeting } from "@/features/work/work-meeting";
export function mapWorkMeetingRow(row: { id: string; project_id: string; meeting_date: string; started_at: string | null; duration_minutes: number; title: string; participants: string | null; agenda: string | null; outcome: string; notes: string | null; archived_at: string | null }, projectTitle: string, followups: WorkMeeting["followups"]): WorkMeeting { return { archivedAt: row.archived_at, agenda: row.agenda, durationMinutes: row.duration_minutes, followups, id: row.id, meetingDate: row.meeting_date, notes: row.notes, outcome: row.outcome, participants: row.participants, projectId: row.project_id, projectTitle, startedAt: row.started_at, title: row.title }; }
