import type { ChallengeProgressLog, ChallengeRecord, RewardLedgerEntry } from "../../domain/challenge";
import type { TableRow } from "../database.types";
export function mapChallenge(row: TableRow<"challenges">): ChallengeRecord { return { archivedAt: row.archived_at, completedAt: row.completed_at, createdAt: row.created_at, description: row.description, endDate: row.end_date, id: row.id, periodType: row.period_type as ChallengeRecord["periodType"], rewardCoins: row.reward_coins, startDate: row.start_date, status: row.status as ChallengeRecord["status"], targetValue: Number(row.target_value), title: row.title, unit: row.unit, updatedAt: row.updated_at }; }
export function mapChallengeLog(row: TableRow<"challenge_progress_logs">): ChallengeProgressLog { return { archivedAt: row.archived_at, challengeId: row.challenge_id, createdAt: row.created_at, id: row.id, increment: Number(row.increment), note: row.note, recordedAt: row.recorded_at, updatedAt: row.updated_at }; }
export function mapRewardLedger(row: TableRow<"reward_ledger_entries">): RewardLedgerEntry { return { amount: row.amount, createdAt: row.created_at, description: row.description, entryType: row.entry_type as RewardLedgerEntry["entryType"], id: row.id, sourceId: row.source_id, sourceType: row.source_type as RewardLedgerEntry["sourceType"] }; }
